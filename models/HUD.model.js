class HUD {
    constructor (canvas, context) {
        this.canvas = canvas;
        this.context = context;
        
        this.currentTime = 0;
        this.timePass = Date.now();
        this.deltaTime = 0;

        // this.Explosions = new 
    }

    drawHUD () {

    }
}